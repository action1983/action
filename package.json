{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:local": "wrangler pages dev dist --port 3000",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name action-report-guide",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name action-report-guide --compatibility-date 2024-01-01",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000 && echo '\n✅ Service is running'",
    "test:api": "curl http://localhost:3000/api/framework | jq . && echo '\n✅ API is working'",
    "setup": "npm install && npm run build",
    "start": "npm run clean-port && npm run build && npm run dev:local",
    "git:commit": "git add . && git commit -m",
    "git:push": "git push origin main",
    "backup": "tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz --exclude=node_modules --exclude=dist --exclude=.git .",
    "format": "prettier --write \"src/**/*.{ts,tsx,js,jsx}\" \"public/**/*.{js,css}\"",
    "lint": "echo 'Linting not configured yet'"
  },
  "dependencies": {
    "hono": "^4.9.2"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}